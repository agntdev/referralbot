[
  {
    "name": "History button shows empty state",
    "steps": [
      {
        "send": { "callback": "history:show" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": {
              "text": "No referrals yet. Tap 📝 New referral to create one."
            }
          }
        ]
      }
    ]
  }
]
